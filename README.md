En este repositorio se encuentra una pequeña calculadora con dos funciones:

- La función x^3, para elevar el número que se escribra en el campo input al cubo.
  Posteriormente se irán añadiendo más funciones.
